#include <stdio.h>

int main()
{
    int i, j;
    int num;
    int a[11][11] = {{1}};
    scanf("%d", &num);

    for(i = 1; i < num; i++)
    {
        for(j = 1; j < num; j++)
        {
            
        }
    }
}